{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build develop docker image",
            "type": "shell",
            "command": "cd ${workspaceFolder} && docker build . -t sd_bot:develop"
        }
    ]
}